name: Code Quality & Linting

on:
  push:
    branches: [ main, develop ]
  pull_request:
    branches: [ main, develop ]

jobs:
  lint:
    runs-on: ubuntu-latest

    steps:
    - uses: actions/checkout@v4

    - name: Set up Python
      uses: actions/setup-python@v4
      with:
        python-version: '3.13'

    - name: Install dependencies
      run: |
        python -m pip install --upgrade pip
        pip install flake8 black isort pylint

    - name: Check code formatting with black
      run: |
        black --check backend/app backend/tests || echo "⚠️  Code formatting issues found (auto-fixable with 'black .')"

    - name: Check import sorting with isort
      run: |
        isort --check-only backend/app backend/tests || echo "⚠️  Import sorting issues found (auto-fixable with 'isort .')"

    - name: Lint with flake8
      run: |
        flake8 backend/app backend/tests --max-line-length=120 --statistics || echo "⚠️  Linting issues found"

    - name: Check with pylint
      continue-on-error: true
      run: |
        pylint backend/app --disable=all --enable=W0611 --max-line-length=150
