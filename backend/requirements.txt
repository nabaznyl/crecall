# Core dependencies
fastapi==0.115.6
uvicorn[standard]==0.34.0
python-multipart==0.0.20

# Database
sqlalchemy==2.0.36
alembic==1.14.0
asyncpg==0.30.0  # PostgreSQL async driver
aiosqlite==0.21.0  # SQLite async driver

# Validation & serialization
pydantic==2.10.6
pydantic-settings==2.7.1

# Authentication & security
python-jose[cryptography]==3.3.0
passlib[bcrypt]==1.7.4
python-dotenv==1.0.1

# Task scheduling
apscheduler==3.11.0

# Utilities
python-dateutil==2.9.0.post0
pytz==2025.1

# Development
pytest==8.3.4
pytest-asyncio==0.24.0
pytest-cov==6.0.0  # Coverage reporting
httpx==0.28.1  # For testing async endpoints
black==25.1.0
ruff==0.9.2
mypy==1.14.1

# Optional: Semantic Search & Caching
# Uncomment to enable advanced features
# sentence-transformers==2.3.1  # Semantic search
# faiss-cpu==1.7.4  # Vector similarity search (or faiss-gpu for CUDA)
# redis==5.0.1  # Caching layer
# numpy==1.26.3  # Required for embeddings

# Optional: Remote Sync (SSH/SFTP)
# paramiko==3.5.0  # SSH/SFTP client for remote synchronization
